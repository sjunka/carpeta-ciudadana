{
  "name": "carpeta-ciudadana-arquitectura",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Documentación de arquitectura del Operador de Carpeta Ciudadana",
  "scripts": {
    "dev": "vite",
    "build": "npm run check && vite build",
    "preview": "vite preview",
    "check": "node scripts/check-content.mjs && node scripts/check-contrast.mjs",
    "artifact": "npm run build && node scripts/build-artifact.mjs"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  },
  "dependencies": {
    "lucide-react": "^1.42.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
